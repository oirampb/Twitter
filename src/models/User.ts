export interface User {
    id?: number;
    nombre?: string;
    email: string;
    password: string;
    location: string;
    webpage: string;
    picture: string;
}
const user = {
    name: "pepe"
};